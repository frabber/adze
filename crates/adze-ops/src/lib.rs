//! Operations and the tool pipe.
//!
//! Every modeling action is a pure, serializable operation over a mesh (D2),
//! composed as action x action centre x falloff x symmetry x snapping (M2).
//! Nothing here is implemented yet; the crate exists so the dependency
//! direction of D18 is enforced by the compiler from the first commit.
